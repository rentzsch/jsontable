{
  "name": "@rentzsch/jsontable",
  "version": "0.1.0",
  "description": "Simple, efficient, flexible, streamable tabular textual data format",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "test": "tsc && ava",
    "demo": "yarn test && webpack && node dist/jsontable-demo-server.js",
    "prepublishOnly": "yarn clean && yarn test"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ava": "^4.1.0",
    "ts-loader": "^9.2.8",
    "type-fest": "^2.12.0",
    "typescript": "^4.6.2",
    "webpack": "^5.71.0",
    "webpack-cli": "^4.9.2"
  },
  "engines": {
    "node": ">=16"
  }
}
