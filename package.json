{
  "name": "@rentzsch/jsontable",
  "version": "0.1.0",
  "description": "Simple, efficient, flexible, streamable tabular textual data format",
  "main": "dist/JSONTable.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "test": "tsc && ava",
    "demo": "tsc && node dist/demo-server/demo-server.js",
    "prepublishOnly": "yarn test && rm dist/**/*.test.js && rm dist/demo-server"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ava": "^4.1.0",
    "type-fest": "^2.12.0",
    "typescript": "^4.6.2"
  },
  "engines": {
    "node": ">=16"
  },
  "files": [
    "dist"
  ]
}
