{
  "name": "@rentzsch/jsontable",
  "version": "0.1.0",
  "description": "Simple, efficient, flexible, streamable tabular textual data format",
  "main": "dist/JSONTable.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "test": "tsc && ava",
    "prepublishOnly": "yarn test && rm -rf dist/test"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ava": "^4.1.0",
    "type-fest": "^2.12.0",
    "typescript": "^4.6.2"
  },
  "engines": {
    "node": ">=10"
  },
  "files": [
    "dist"
  ]
}
