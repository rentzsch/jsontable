{
  "name": "@rentzsch/jsontable",
  "version": "0.1.0",
  "description": "Simple, efficient, flexible, streamable tabular textual data format",
  "main": "dist/jsontable.js",
  "license": "MIT",
  "scripts": {
    "clean": "find src -name '*.js' -delete",
    "test": "tsc && ava",
    "demo": "yarn test && webpack && node src/jsontable-demo-server.js",
    "prepublishOnly": "yarn clean && yarn test && rm dist/**/*.test.js && rm dist/demo-server"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "ava": "^4.1.0",
    "ts-loader": "^9.2.8",
    "ts-node": "^10.7.0",
    "tslib": "^2.3.1",
    "type-fest": "^2.12.0",
    "typescript": "^4.6.2",
    "webpack": "^5.71.0",
    "webpack-cli": "^4.9.2"
  },
  "engines": {
    "node": ">=16"
  },
  "files": [
    "src/**/*.js"
  ]
}
